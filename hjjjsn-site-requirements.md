# hjjjsn アーティストサイト 要件定義書

コーディングエージェントへ:このドキュメントはサイトの唯一の仕様書である。実装判断に迷ったら「§2 ブランド定義」に立ち返り、そこに書かれた人物像に合致する方を選ぶこと。

---

## 1. 目的とファネル設計

### 1.1 サイトの唯一の仕事

YouTube / niconico / TikTok という「大通り」から流入した人を受け止め、hjjjsn という人間を深く理解してもらい、最終的に Discord でつながってもらうこと。

```
[大通り]                     [このサイト]                    [終点]
YouTube / niconico  ──→  作品を観る → 人物を知る → 言葉を読む ──→  Discord 参加
TikTok / X                (Works)     (About)     (Words)          (CTA)
```

### 1.2 導線の原則

- 流入者は「1本のMV/曲を見て来た人」と想定する。ランディング直後に「その作者はこういう人間だ」が伝わること。
- Discord への誘導は最奥に置く。押し売りせず、サイトを回遊して人物像に触れた人が自然にたどり着く「奥の部屋」として設計する。
- 各コンテンツ(note記事、YouTube動画、niconico動画)は外部プラットフォームに実体があり、サイトはそれらを文脈づけて並べる「編集された入口」である。

### 1.3 成功条件

- 新しいコンテンツ(曲、MV、note記事)を **markdownファイル1個の追加だけ** で掲載できる
- サイト運用に月額コストがかからない
- 訪問者がスマートフォンで見ても世界観が崩れない(流入元がYouTube/TikTokなのでモバイル比率が高い前提)

---

## 2. ブランド定義 — 伝えるべき人物像

エッセイ集『溶けているあいだ』を一次資料とする。このサイトが伝えるのは以下の人物である。

### 2.1 核

**体が先に引っかかって、あとから言葉がついてくる人。**

- 感覚や違和感を「なかったことにしない」。うまく言えなくても、引っかかったまま外に置く
- 作品はまず自己定着(立ち上がった知覚を消えないうちに言葉にすること)のためにある。読者向けに核を矯正することはしない。ただし「同じ核に別の入口を与える」推敲はする
- 溶けて、析出して、また溶ける。循環の記録。ひとつの作品が次の作品を呼ぶ

### 2.2 領域の広がり

音楽(ボカロP / 変拍子の math rock)、批評・エッセイ、イラスト・アニメーション、DSP・プラグイン開発。ジャンル横断だが「知覚を定着させる」という一本の芯が通っている、と見せること。「多才です」ではなく「同じことを別の素材でやっている」という提示。

### 2.3 トーン

- 静か、内省的、しかし観察は鋭利。柔らかいが甘くない
- 誇張しない。煽らない。「!」を使わない
- 一人称の距離感:近すぎない。読者に語りかけるより、置かれた言葉を読者が拾う形
- NG:キラキラした自己紹介、実績の羅列で圧をかける構成、ポップで元気なトーン、絵文字

### 2.4 コピーの例(トーンの基準として)

- Discord CTA:「たまに、ここで話しています」程度の温度。「参加しよう!」ではない
- About 冒頭:『溶けているあいだ』まえがきの一文「体が先に引っかかって、あとから言葉がついてくる」をそのまま使ってよい(自著のため)

---

## 3. ビジュアル仕様

### 3.1 デザインコンセプト:「溶けているあいだ」

車窓の波打ち、瞼の裏の白濁、繭、薄緑のモノクロ、焦点が合う瞬間 — エッセイ集の身体感覚を視覚言語に翻訳する。**「ぼやけたものに焦点が合う」** がサイト全体のシグネチャモーションである。

### 3.2 カラートークン

低コントラスト・低彩度を基調とし、可読性が必要な箇所(本文)のみコントラストを確保する。

| トークン | 値(目安) | 用途 |
|---|---|---|
| `--base` | `#EDEDE8` | 背景。白濁した生成り。純白は使わない |
| `--ink` | `#3A3D3A` | 本文。純黒は使わない(墨がにじんだグレー) |
| `--haze` | `#C8CCC4` | 罫線、境界、非活性。薄緑がかったグレー(「薄緑のモノクロ」) |
| `--moss` | `#6B7A6E` | 見出し補助、リンクホバー、メタ情報 |
| `--precipitate` | `#B5543F` | 一点のみのアクセント(析出)。Discord CTA と現在地表示にのみ使用 |

- グラデーション禁止。影は使うとしても `--haze` の極薄いもの
- ダークモード対応は不要(初版スコープ外)。世界観は単一の光で統一する

### 3.3 タイポグラフィ

| 役割 | 書体 | 備考 |
|---|---|---|
| 見出し・タイトル | **Zen Old Mincho** (Google Fonts) | オールドスタイル明朝。文学的な骨格 |
| 本文 | **Zen Kaku Gothic New** | 静かなゴシック。長文の可読性 |
| メタ情報・英字 | **IBM Plex Mono** | 日付、タグ、技術的ラベル。DSP/エンジニア面の記号 |

- 見出しは大きく・細く・字間広め(`letter-spacing: 0.08em` 程度)
- サイトロゴ/タイトル「hjjjsn」は小文字のまま、IBM Plex Mono で控えめに
- 縦書き(`writing-mode: vertical-rl`)をトップページの一箇所だけ使う(§3.5)。多用しない

### 3.4 シグネチャモーション:「析出」

- ページ読み込み時・スクロールによる要素の出現を、**`filter: blur(8px)` + 低opacity → blur(0) + opacity 1** のトランジション(600ms, ease-out)で統一する。フェードインでもスライドインでもなく、「焦点が合う」こと
- ホバーは逆方向:サムネイルにホバーすると極わずかに(2px)にじむ。「触れると溶ける」
- `prefers-reduced-motion: reduce` では全モーションを即時表示に切り替えること(必須)
- 上記以外の装飾的アニメーションは追加しない。動きはこの一種類に絞る

### 3.5 レイアウト構造

```
┌────────────────────────────────┐
│ hjjjsn                    (nav) │  ← ヘッダは極小。mono体
├────────────────────────────────┤
│                            ┃体 │
│   [代表作MVの埋め込み]      ┃が │  ← ヒーロー:最新or代表MV 1本
│                            ┃先 │     + 縦書きの一文(右端)
│                            ┃に │
├────────────────────────────────┤
│ Works                           │
│ [曲] [曲] [曲]                  │  ← カードは静的。余白多め
│                                 │
│ Words                           │
│ ・エッセイタイトル ── note      │  ← リスト形式。書影1点のみ画像
│ ・『溶けているあいだ』          │
│                                 │
│ About(抜粋) → 詳細ページへ      │
├────────────────────────────────┤
│ ── たまに、ここで話しています   │  ← Discord CTA(--precipitate)
│    [Discord]                    │
│ YouTube / niconico / X /        │
│ Bandcamp / BOOTH / note         │  ← フッタにリンク一覧
└────────────────────────────────┘
```

- 余白は「繭」。セクション間は詰めない(モバイルでも `6rem` 以上)
- 罫線は `--haze` の1px。カードに枠や影をつけず、余白と罫線だけで区切る
- グリッドは規則的に(数学的・変拍子の精度)、その上に載る要素はにじむ(身体感覚)。この対比がこのサイトの設計思想

---

## 4. ページ構成と機能要件

### 4.1 サイトマップ

| パス | ページ | 役割 |
|---|---|---|
| `/` | トップ | ヒーローMV + Works/Words 抜粋 + Discord CTA |
| `/works/` | 作品一覧 | 音楽・MV・イラスト等。タグでフィルタ |
| `/works/[slug]/` | 作品詳細 | 埋め込み + 制作の言葉 + 関連リンク(BOOTH/Bandcamp等) |
| `/words/` | 言葉一覧 | note記事リンク、書籍、サイト内エッセイ |
| `/about/` | 人物 | 人物像、活動領域、機材/技術(任意)、連絡先 |
| `/links/` | リンク集 | 全プラットフォームへのリンク(SNSプロフィールから直接貼る用) |

### 4.2 コンテンツモデル(最重要)

コンテンツはすべて **リポジトリ内の markdown + frontmatter** で管理する。CMS・DBは使わない。

```
src/content/
  works/
    2026-04-ame-ga-agattara.md
  words/
    2026-03-tokete-iru-aida.md
```

works の frontmatter スキーマ(zodで型検証すること):

```yaml
title: "雨があがったら"
date: 2026-04-01
type: music            # music | mv | illustration | plugin
platform: youtube      # youtube | niconico | none
embedId: "XXXXXXXX"    # 埋め込み用ID
links:
  - label: "Bandcamp"
    url: "https://..."
  - label: "BOOTH"
    url: "https://..."
tags: ["math rock", "vocaloid"]
featured: true         # トップのヒーローに使うか
description: "一行紹介"
```

words の frontmatter:

```yaml
title: "溶けているあいだ"
date: 2026-03-01
kind: book             # book | note | essay(サイト内)
url: "https://note.com/..."   # 外部の場合
description: "エッセイ集。まえがきの一文を引用してよい"
```

- 本文(markdown body)は「制作の言葉」として詳細ページに表示。空でもよい
- **新規コンテンツ追加の手順が「mdファイルを1個置いて git push」だけになること**を受け入れ条件とする

### 4.3 埋め込み仕様

- YouTube: `youtube-nocookie.com` を使い、**クリックするまでiframeを読み込まないファサード**(サムネイル画像 + 再生ボタン)にする。パフォーマンスと世界観(静けさ)の両方のため
- niconico: 外部プレイヤー埋め込み(`embed.nicovideo.jp`)。同様にクリックで読み込み
- 埋め込みサムネイルの上に `--haze` の薄いオーバーレイを敷き、ホバーで晴れる(§3.4のにじみと統一)

### 4.4 Discord CTA

- トップ最下部と `/about/`、`/links/` に設置
- 文言はトーン基準(§2.4)に従う。ボタンは `--precipitate` の細い枠線 + テキスト。塗りつぶしの大きなボタンにしない
- 招待リンクは環境変数ではなく `src/config.ts` の定数として一元管理(全外部リンクも同様に一元管理)

### 4.5 非機能要件

- OGP/Twitterカード:全ページに設定。OG画像は `--base` 地に明朝でタイトルを置いた静的画像を自動生成(satori等)またはデフォルト1枚
- Lighthouse Performance 95以上(モバイル)。埋め込みファサード化で達成可能
- アクセシビリティ:キーボードフォーカス可視、コントラストは本文でWCAG AA(`--ink` on `--base` は約8:1で満たす)、`prefers-reduced-motion` 対応
- 解析:入れる場合は Cloudflare Web Analytics か GoatCounter(cookieレス)。Google Analyticsは使わない
- RSS(`/rss.xml`)を works + words 統合で出力

---

## 5. 技術選定

### 5.1 フレームワーク:**Astro + TypeScript**(React全面採用はしない)

| 観点 | Astro | Next.js (React) | Vite + React SPA |
|---|---|---|---|
| コンテンツ管理 | ◎ Content Collections + zod型検証が標準 | ○ 自前実装 | △ 自前実装 |
| 出力 | ◎ 完全静的・JSゼロが基本 | ○ 静的exportは可能だが本領でない | △ SPAはSEO/OGPに不利 |
| 今回の要件との適合 | ◎ 「mdを置いたら1ページ増える」がそのまま実現 | 過剰 | 不足 |

- このサイトは**コンテンツが主でインタラクションが従**。Reactが解く問題(複雑な状態管理)が存在しないため、React を土台にする理由がない
- ただし Astro は必要な箇所だけ React コンポーネントを island として差し込める。将来インタラクティブな要素(例:Web Audio を使った作品ページ)を作りたくなったら、そのページにだけ React を導入すればよい。**「TypeScriptは全面採用、Reactは必要になった島だけ」**が結論
- スタイリング:プレーンCSS(CSS変数でトークン管理)または Tailwind。§3のトークンを CSS カスタムプロパティとして `:root` に定義することを必須とする

### 5.2 ホスティング:**Cloudflare Pages**(第一候補)/ GitHub Pages(次点)。Firebase は不採用

| 観点 | Cloudflare Pages | GitHub Pages | Firebase Hosting |
|---|---|---|---|
| 費用 | 無料 | 無料 | 無料枠あり(要Google課金設定) |
| デプロイ | git push で自動 | git push + Actions | CLI or CI 設定 |
| 独自ドメイン/SSL | ◎ | ○ | ○ |
| 静的サイトへの適合 | ◎ CDN高速・帯域無制限 | ○ | ○ だが過剰 |
| 将来の拡張 | Workers/Functionsを後付け可 | 静的のみ | Auth/DB等が使えるが今回不要 |

- **Firebase を選ぶ理由が今回は存在しない。** Firebase の価値は Auth・Firestore・Functions とのセットにあり、静的サイト+外部リンクだけの本件では管理コンソールとgcloudツールチェーンが純粋なオーバーヘッドになる
- GitHub Pages でも十分成立するが、Cloudflare Pages は同じ「git push だけ」の運用でビルド設定がGUIで完結し、プレビューデプロイ(PRごとの確認URL)が標準で付く。コーディングエージェントと反復開発する運用に向く
- 独自ドメイン(例: `hjjjsn.com` / `.jp` / `.art`)の取得を推奨。プラットフォームに依存しない恒久的な住所として、全SNSプロフィールに貼る URL はこのドメインに統一する

### 5.3 リポジトリ構成(指示)

```
/
├─ astro.config.mjs
├─ src/
│  ├─ config.ts            # 全外部リンク・Discord招待・サイト定数
│  ├─ styles/tokens.css    # §3 のデザイントークン
│  ├─ content/
│  │  ├─ config.ts         # zodスキーマ(§4.2)
│  │  ├─ works/
│  │  └─ words/
│  ├─ components/          # EmbedFacade, WorkCard, WordRow, DiscordCta 等
│  ├─ layouts/Base.astro   # OGP・フォント読み込み・ヘッダ/フッタ
│  └─ pages/               # §4.1 のルーティング
└─ public/                 # OG画像、favicon、書影
```

---

## 6. 実装フェーズと受け入れ条件

**Phase 1(初版リリース)**
1. トークン・レイアウト・全6ページの静的実装(コンテンツはサンプル3件で可)
2. 埋め込みファサード、析出モーション、reduced-motion 対応
3. Cloudflare Pages デプロイ + 独自ドメイン接続

受け入れ条件:
- [ ] mdファイル追加のみで works / words に項目が増える
- [ ] モバイル(375px)で全ページのレイアウトが崩れない
- [ ] Lighthouse(モバイル) Performance 95+ / Accessibility 95+
- [ ] JSを無効にしてもテキスト・リンク・画像がすべて閲覧できる(埋め込み再生のみ不可で可)
- [ ] `--precipitate` 色が Discord CTA と現在地表示以外に使われていない

**Phase 2(以降、必要に応じて)**
- OG画像の自動生成
- タグフィルタ(works)
- サイト内エッセイ(words の kind: essay)の本文ページ
- Web Audio 等を使うインタラクティブ作品ページ(React island として)
